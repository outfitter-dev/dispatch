{
  "mcpServers": {
    "dispatch": {
      "command": "dispatch",
      "args": ["mcp"],
      "note": "Local dispatch MCP server. Exposes the same op registry as the dispatch CLI; run `dispatch up` before tool calls that need the daemon."
    }
  }
}
